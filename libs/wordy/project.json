{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/wordy/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/wordy",
        "main": "libs/wordy/src/index.ts",
        "tsConfig": "libs/wordy/tsconfig.lib.json",
        "assets": ["libs/wordy/*.md"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/wordy/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/wordy"],
      "options": {
        "jestConfig": "libs/wordy/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
